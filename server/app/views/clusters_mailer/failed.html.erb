<%#
  Copyright (C) 2017 Stephen F. Norledge and Alces Software Ltd.

  This file is part of Alces Launch.

  All rights reserved, see LICENSE.txt.
%>
<% setup %>

<p>Hi,</p>

<p>
Unfortunately, your cluster "<%= @cluster_name %>" failed to launch.  You can
view the error message below.  If this problem continues, please contact
<a href="mailto:support@alces-flight.com">support@alces-flight.com</a>.
</p>

<% if @show_cloudformation_link %>
<% if @arn_present %>
    <p>
    You should visit your cluster's <a href="<%= @cloudformation_url %>">AWS
        CloudFormation page</a> and terminate it.
    </p>
<% else %>
    <p>
    You should visit the <a href="<%= @cloudformation_url %>">AWS
        CloudFormation console</a> and make sure that the cluster is not
    running.
    </p>
<% end %>
<% end %>

<blockquote>
    <table cellpadding="0" cellspacing="0" border="0">
        <thead>
            <tr>
                <td width="<%= td_width(1) %>">
                    <h4 class="error-message-title">Error message</h4>
                </td>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td width="<%= td_width(1) %>">
                    <pre><%= @stderr %></pre>
                </td>
            </tr>
        </tbody>
    </table>
</blockquote>

<% note do %>
    You're receiving this email because you have started a cluster with Flight
    Launch, and that cluster has failed to launch.
<% end %>

<%= render 'shared/contact_us' %>
