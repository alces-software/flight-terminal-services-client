{
  "name": "flight-launch",
  "description": "The Flight Launch web service",
  "repository": "https://github.com/alces-software/flight-launch",
  "scripts": {
    "dokku": {
      "predeploy": "wget -q $FLY_DOWNLOAD_URL --output-document $FLY_EXE_PATH ; chmod +x $FLY_EXE_PATH; wget -q $FLY_NEXT_DOWNLOAD_URL --output-document $FLY_NEXT_EXE_PATH ; chmod +x $FLY_NEXT_EXE_PATH"
    }
  }
}
